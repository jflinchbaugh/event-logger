Instructions: Do not modify whitespace unnecessarily.

Tests are run in the browser, so don't try to run them on the command line.
To check syntax, compile the frontend only: npx shadow-cljs compile frontend
